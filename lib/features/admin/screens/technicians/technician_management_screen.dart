import 'package:flutter/material.dart';

class TechnicianManagementScreen extends StatelessWidget {
  const TechnicianManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Gestion des techniciens')),
      body: const Center(child: Text('Gestion des techniciens')),
    );
  }
}
